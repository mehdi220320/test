name: untitled
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.2
  cupertino_icons: ^1.0.8
  google_mlkit_object_detection: ^0.15.0
  tflite_flutter: ^0.10.0
  vector_math: ^2.1.4
  image: ^4.0.17
  ar_flutter_plugin: ^0.8.0  # Alternative AR plugin
  permission_handler: ^11.0.1  # For handling permissions

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/
    - assets/labels.txt
